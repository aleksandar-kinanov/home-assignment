{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.fullname" . }}-grafana
  labels:
    {{- include "app.componentLabels" (dict "ctx" . "component" "grafana") | nindent 4 }}
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.monitoring.grafana.service.nodePort }}
  selector:
    {{- include "app.componentSelectorLabels" (dict "ctx" . "component" "grafana") | nindent 4 }}
{{- end }}
